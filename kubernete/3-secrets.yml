# ATTENTION: Ce fichier est un template
# Les secrets doivent être créés via le script create-secrets.ps1
# Les données sensibles ne doivent JAMAIS être en clair dans le contrôle de version

# Générer les secrets avec:
# ./create-secrets.ps1

# Vérifier les secrets créés:
# kubectl get secrets -n app
# kubectl describe secret mysql-secret -n app
