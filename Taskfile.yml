version: '3'

tasks:
  up:
    cmds:
      - docker compose up -d

  down:
    cmds:
      - docker compose down

  build:
    cmds:
      - docker compose build

  logs:
    cmds:
      - docker compose logs -f
