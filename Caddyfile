:80 {

    # API Backend (Node.js)
    handle /api/* {
        uri strip_prefix /api
        reverse_proxy backend:5000
    }

    # Frontend Vue.js (Vite)
    handle {
        reverse_proxy frontend:5173
    }
}
